# Copy to .env and fill in from your Supabase project:
#   Supabase dashboard -> Project Settings -> API
#
# Leave these blank and the app runs entirely on browser localStorage,
# which is handy for a first run but does not sync between devices.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
